1. extract the content in your prefered place
2. install vs code, and node.js
3. open the monster_handler_dnd_main using vs code (drag the folder to vs code)
4. in the terminal (ctrl+shift+p choose create new terminal) run the startApp.bat (the server will run)
5. add live server to the extentions
6. right click the client\pages\home.html and click open live server

7. enjoy
